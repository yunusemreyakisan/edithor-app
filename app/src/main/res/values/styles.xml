<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="AppSearchView" parent="Widget.AppCompat.SearchView">
        <!-- Query Text Color -->
        <item name="android:textColorPrimary">@color/md_theme_light_primary</item>
        <!-- Hint Color -->
        <item name="android:textColorHint">@color/md_theme_light_primary</item>
        <!-- Icon Color -->
        <item name="android:tint">@color/md_theme_light_primary</item>
    </style>

    <style name="ToolbarTheme" parent="@style/ThemeOverlay.AppCompat.ActionBar">
        <!-- Customize color of navigation drawer icon and back arrow -->
        <item name="colorControlNormal">@color/md_theme_light_primary</item>
    </style>
</resources>